/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // images: { unoptimized: true }, // Hapus tanda // di depan images jika Anda memakai tag <Image> Next.js
};

export default nextConfig;